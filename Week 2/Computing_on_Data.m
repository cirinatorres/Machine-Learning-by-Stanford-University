A = [ 1 2; 3 4; 5 6];B = [11 12; 13 14; 15 16];C = [1 1; 2 2];A .* B	% 'Element wise operation': Multiplication of each element of A		% for the corresponding element of B.		A .^2v = [1; 2; 3];1 ./ vvv + ones(length(v),1)w = [1 2 3];w + ones(1,length(w))A ~= (A')'[val, max] = max(v)	% obtaining the max value and its index2 < v	% evals for every feature in the vectorfind(2 < v)	% to obtain the indexes of those who eval to 1find(2 < A)	% returns positions as in a vector[r c] = find(2 < A);	% returns positions row & columnr = [r [c]];rA = magic(3);Amax(A,[],1)	% column wise maximumflipud(eye(3))	% Mirrors the parameter